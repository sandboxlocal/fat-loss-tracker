"use strict";
const U=TrackerUtils,app=document.querySelector("#app"),toast=document.querySelector("#toast");
const state={view:"today",date:U.localDateString(),settings:null,logs:[],checkins:[],updateReady:false};
const defaults={key:"plan",startingWeight:"",checkpointWeight:"",dailyCalories:1800,weeklyCalories:12600,proteinTarget:100,stepTarget:7000,liftMin:3,liftMax:4,lossMin:.5,lossMax:1.5,startDate:U.localDateString(),weekStart:0,unit:"lb"};
const esc=(v="")=>String(v).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
const fmt=(v,d=0)=>v==null||Number.isNaN(v)?"—":Number(v).toLocaleString(undefined,{minimumFractionDigits:d,maximumFractionDigits:d});
const signed=(v,s="")=>v==null?"—":`${v>0?"+":""}${fmt(v,1)}${s}`;
const dateFmt=(v,o={weekday:"long",month:"long",day:"numeric"})=>U.parseLocalDate(v).toLocaleDateString(undefined,o);
function notify(m){toast.textContent=m;toast.classList.add("show");clearTimeout(notify.t);notify.t=setTimeout(()=>toast.classList.remove("show"),2400)}
async function load(){state.settings=await TrackerDB.get("settings","plan");state.logs=(await TrackerDB.getAll("logs")).sort((a,b)=>a.date.localeCompare(b.date));state.checkins=(await TrackerDB.getAll("checkins")).sort((a,b)=>a.date.localeCompare(b.date))}
function nav(content){return `<div class="app-shell"><header class="app-header"><div><i></i><span>Personal Progress</span></div>${state.updateReady?'<button data-action="update">Update available</button>':""}</header><main class="content">${content}</main><nav class="bottom-nav" aria-label="Primary navigation">${[["today","Today"],["progress","Progress"],["checkin","Check-In"],["plan","Plan"]].map(([id,label])=>`<button data-view="${id}" class="${state.view===id?"active":""}" aria-current="${state.view===id?"page":"false"}"><b class="ico ${id}"></b><span>${label}</span></button>`).join("")}</nav></div>`}
function onboarding(){app.innerHTML=`<main class="onboarding"><div class="onboarding-art"><i></i><i></i><i></i></div><p class="eyebrow">Private by design</p><h1>A calm place to see your progress.</h1><p class="lede">Your plan and records stay on this device. Nothing is sent to an account, analytics service, or server.</p><form id="setup-form" class="setup-form"><section class="setup-card"><header><b>01</b><div><h2>Starting point</h2><p>Edit any value later.</p></div></header><div class="two"><label>Starting weight <small>lb</small><input required inputmode="decimal" type="number" min="1" step=".1" name="startingWeight" placeholder="Enter weight"></label><label>First checkpoint <small>lb</small><input required inputmode="decimal" type="number" min="1" step=".1" name="checkpointWeight" placeholder="Reassessment weight"></label></div><label>Tracking start date<input required type="date" name="startDate" value="${defaults.startDate}" max="${U.localDateString()}"></label></section><section class="setup-card"><header><b>02</b><div><h2>Daily rhythm</h2><p>Generic suggestions, not personal recommendations.</p></div></header><div class="two"><label>Daily calories<input required inputmode="numeric" type="number" min="1" name="dailyCalories" value="${defaults.dailyCalories}"></label><label>Weekly calories<input required inputmode="numeric" type="number" min="1" name="weeklyCalories" value="${defaults.weeklyCalories}"></label><label>Protein <small>g</small><input required inputmode="numeric" type="number" min="1" name="proteinTarget" value="${defaults.proteinTarget}"></label><label>Steps<input required inputmode="numeric" type="number" min="1" name="stepTarget" value="${defaults.stepTarget}"></label></div></section><section class="setup-card"><header><b>03</b><div><h2>Weekly intentions</h2><p>Choose useful ranges.</p></div></header><div class="two"><label>Lift days, minimum<input required inputmode="numeric" type="number" min="0" max="7" name="liftMin" value="3"></label><label>Lift days, maximum<input required inputmode="numeric" type="number" min="0" max="7" name="liftMax" value="4"></label><label>Loss range, lower <small>lb</small><input required inputmode="decimal" type="number" min="0" step=".1" name="lossMin" value=".5"></label><label>Loss range, upper <small>lb</small><input required inputmode="decimal" type="number" min="0" step=".1" name="lossMax" value="1.5"></label></div></section><button class="primary" type="submit">Begin tracking</button><p class="privacy">Stored only in this browser. Export backups regularly.</p></form></main>`}
function metric(label,value,target,unit,tone){const v=Number(value||0),t=Number(target||0),pct=t?Math.min(100,v/t*100):0;return `<article class="metric ${tone}"><div><span>${label}</span><strong>${fmt(v)} <small>/ ${fmt(t)}${unit?` ${unit}`:""}</small></strong></div><div class="track" role="progressbar" aria-label="${label}: ${v} of ${t}"><i style="width:${pct}%"></i></div></article>`}
function logFor(d=state.date){return state.logs.find(x=>x.date===d)||{date:d,weight:"",calories:"",protein:"",steps:"",lifted:false,note:""}}
function today(){const l=logFor(),b=U.weeklyBudget(state.logs,state.settings,state.date),last=state.checkins.at(-1),due=!last||U.daysBetween(last.date,U.localDateString())>=14,count=state.logs.filter(x=>x.date<=U.localDateString()).length,canNext=state.date<U.localDateString();app.innerHTML=nav(`<section class="today-head"><div><p class="eyebrow">${state.date===U.localDateString()?"Today":"Daily log"}</p><h1>${dateFmt(state.date)}</h1><p>${count} day${count===1?"":"s"} logged · consistency over perfection</p></div><div class="date-nav"><button data-action="prev-day" aria-label="Previous day">‹</button><button data-action="today-date">Today</button><button data-action="next-day" ${canNext?"":"disabled"} aria-label="Next day">›</button></div></section>${due?'<button class="due-banner" data-view="checkin"><span><strong>Body check-in is due</strong><small>A gentle two-week reminder</small></span><b>›</b></button>':""}<section class="metrics">${metric("Calories",l.calories,state.settings.dailyCalories,"","rose")}${metric("Protein",l.protein,state.settings.proteinTarget,"g","mauve")}${metric("Steps",l.steps,state.settings.stepTarget,"","sage")}</section><section class="weekly"><header><div><p class="eyebrow">Weekly budget</p><h2>${fmt(b.remaining)} <small>calories ${b.remaining>=0?"left":"over"}</small></h2></div><span>${dateFmt(b.start,{month:"short",day:"numeric"})}–${dateFmt(b.end,{month:"short",day:"numeric"})}</span></header><div class="budget-bar"><i style="width:${b.budget?Math.min(100,b.consumed/b.budget*100):0}%"></i></div><footer><span><strong>${fmt(b.consumed)}</strong> of ${fmt(b.budget)} eaten</span><span><strong>${fmt(b.perDay)}</strong>/day for ${b.remainingDays} remaining day${b.remainingDays===1?"":"s"}</span></footer></section><form id="daily-form" class="form-card"><header class="card-head"><div><p class="eyebrow">Daily details</p><h2>Quick log</h2></div>${state.logs.some(x=>x.date===state.date)?'<span class="saved">Saved</span>':""}</header><div class="two"><label>Morning weight <small>${state.settings.unit}</small><input inputmode="decimal" type="number" min="1" step=".1" name="weight" value="${esc(l.weight??"")}" placeholder="—"></label><label>Calories eaten<input inputmode="numeric" type="number" min="0" name="calories" value="${esc(l.calories??"")}" placeholder="—"></label><label>Protein <small>g</small><input inputmode="numeric" type="number" min="0" name="protein" value="${esc(l.protein??"")}" placeholder="—"></label><label>Steps<input inputmode="numeric" type="number" min="0" name="steps" value="${esc(l.steps??"")}" placeholder="—"></label></div><fieldset><legend>Lifted today?</legend><label><input type="radio" name="lifted" value="false" ${!l.lifted?"checked":""}><span>No</span></label><label><input type="radio" name="lifted" value="true" ${l.lifted?"checked":""}><span>Yes</span></label></fieldset><label>Optional note<textarea name="note" rows="3" maxlength="500" placeholder="Anything worth remembering?">${esc(l.note)}</textarea></label><button class="primary" type="submit">Save Today</button></form>`)}
function draw(canvas,series,colors){if(!canvas||!series.some(s=>s.length))return;const r=canvas.getBoundingClientRect(),d=devicePixelRatio||1;canvas.width=r.width*d;canvas.height=r.height*d;const c=canvas.getContext("2d");c.scale(d,d);const all=series.flat().map(x=>x.value),min=Math.min(...all),max=Math.max(...all),spread=max-min||1,p=20,w=r.width-p*2,h=r.height-p*2;c.strokeStyle="#eadfda";for(let i=0;i<4;i++){let y=p+h*i/3;c.beginPath();c.moveTo(p,y);c.lineTo(r.width-p,y);c.stroke()}series.forEach((s,j)=>{c.strokeStyle=colors[j];c.lineWidth=2.5;c.lineJoin="round";c.beginPath();s.forEach((x,i)=>{let px=p+w*i/Math.max(1,s.length-1),py=p+h*(1-(x.value-min)/spread);i?c.lineTo(px,py):c.moveTo(px,py)});c.stroke()})}
function progress(){const weeks=U.groupWeeks(state.logs,state.settings),weighted=weeks.filter(w=>w.averageWeight!=null).slice(0,12).reverse(),current=weighted.at(-1)?.averageWeight??U.average(state.logs.slice(-7).map(x=>x.weight)),cp=U.checkpointProgress(state.settings,current);app.innerHTML=nav(`<section class="page-head"><p class="eyebrow">Progress</p><h1>The trend, gently.</h1><p>Weekly averages soften the noise of daily fluctuations.</p></section>${cp?.reached?'<section class="reached"><p class="eyebrow">Checkpoint reached</p><h2>You’ve reached the point you planned to reassess.</h2><p>Review your progress before deciding what comes next.</p></section>':""}<section class="overview"><div><span>Current weekly average</span><strong>${fmt(current,1)} <small>${state.settings.unit}</small></strong><p>${cp?`${fmt(cp.lost,1)} ${state.settings.unit} moved · ${fmt(cp.remaining,1)} ${state.settings.unit} remaining`:"Log weights to begin your trend."}</p></div><div class="ring" style="--v:${cp?.percent||0}"><b>${fmt(cp?.percent||0)}%</b><span>to checkpoint</span></div></section><section class="chart-card"><header class="card-head"><div><p class="eyebrow">Weight trend</p><h2>Weekly average</h2></div><span>${weighted.length} weeks</span></header>${weighted.length?'<canvas id="weight-chart" height="220" aria-label="Weekly average weight chart"></canvas>':'<div class="empty"><strong>Your chart will grow here.</strong><p>Log morning weights to reveal a steadier trend.</p></div>'}</section><section class="summaries"><div class="section-title"><p class="eyebrow">Weekly summaries</p><h2>Recent weeks</h2></div>${weeks.length?weeks.slice(0,8).map(w=>`<article class="week"><header><div><strong>${dateFmt(w.start,{month:"short",day:"numeric"})}–${dateFmt(w.end,{month:"short",day:"numeric"})}</strong><span>${w.complete?w.pace:"In progress"}</span></div><b>${fmt(w.averageWeight,1)} <small>${state.settings.unit} avg</small></b></header><div class="summary-grid"><span><small>Weight change</small><strong>${w.change==null?"Pending":signed(w.change,` ${state.settings.unit}`)}</strong></span><span><small>Calories</small><strong>${fmt(w.totalCalories)}</strong></span><span><small>Protein avg</small><strong>${fmt(w.averageProtein)} g</strong></span><span><small>Steps avg</small><strong>${fmt(w.averageSteps)}</strong></span><span><small>Lifting</small><strong>${w.liftingDays} days</strong></span><span><small>Days logged</small><strong>${w.daysLogged} / 7</strong></span></div><p>${w.calorieBalance>=0?`${fmt(w.calorieBalance)} calories remaining`:`${fmt(Math.abs(w.calorieBalance))} calories over budget`}</p></article>`).join(""):'<div class="empty"><strong>No weekly summaries yet.</strong><p>Your first week will appear as you log.</p></div>'}</section>`);requestAnimationFrame(()=>draw(document.querySelector("#weight-chart"),[weighted.map(w=>({value:w.averageWeight}))],["#a96875"]))}
function checkin(){const latest=state.checkins.at(-1),first=state.checkins[0],due=!latest||U.daysBetween(latest.date,U.localDateString())>=14;app.innerHTML=nav(`<section class="page-head"><p class="eyebrow">Body check-in</p><h1>Notice the longer story.</h1><p>A check-in every two weeks is enough. Tiny shifts don’t need a big interpretation.</p></section>${due?'<section class="due-card"><i></i><div><strong>Body check-in is due</strong><p>Your last check-in was at least 14 days ago.</p></div></section>':`<section class="next"><span>Next suggested check-in</span><strong>${dateFmt(U.addDays(latest.date,14),{month:"long",day:"numeric"})}</strong></section>`}<form id="checkin-form" class="form-card"><header class="card-head"><div><p class="eyebrow">New check-in</p><h2>Measurements</h2></div></header><label>Date<input required type="date" name="date" max="${U.localDateString()}" value="${U.localDateString()}"></label><div class="two"><label>Weight <small>${state.settings.unit}</small><input required inputmode="decimal" type="number" min="1" step=".1" name="weight"></label><label>Right upper arm <small>in</small><input required inputmode="decimal" type="number" min="0" step=".01" name="rightArm"></label><label>Left upper arm <small>in</small><input required inputmode="decimal" type="number" min="0" step=".01" name="leftArm"></label><label>Waist <small>in</small><input required inputmode="decimal" type="number" min="0" step=".01" name="waist"></label></div><label>Optional note<textarea name="note" rows="3" maxlength="500"></textarea></label><button class="primary">Save Check-In</button></form>${latest?`<section class="arm-card"><div class="section-title"><p class="eyebrow">Arm measurements</p><h2>First to latest</h2></div><div class="arm-grid"><article><span>Right arm</span><strong>${fmt(latest.rightArm,2)} in</strong><p>${signed(latest.rightArm-first.rightArm," in")} since first</p></article><article><span>Left arm</span><strong>${fmt(latest.leftArm,2)} in</strong><p>${signed(latest.leftArm-first.leftArm," in")} since first</p></article></div><canvas id="arm-chart" height="190"></canvas></section>`:""}<section class="history"><div class="section-title"><p class="eyebrow">History</p><h2>Previous check-ins</h2></div>${state.checkins.length?state.checkins.slice().reverse().map(x=>`<article class="check-row"><div><strong>${dateFmt(x.date,{month:"short",day:"numeric",year:"numeric"})}</strong><span>${fmt(x.weight,1)} ${state.settings.unit} · Waist ${fmt(x.waist,2)} in</span></div><div><span>R ${fmt(x.rightArm,2)}</span><span>L ${fmt(x.leftArm,2)}</span><button data-action="delete-checkin" data-date="${x.date}" aria-label="Delete check-in">×</button></div></article>`).join(""):'<div class="empty"><strong>Your first comparison starts here.</strong><p>Add a check-in whenever you’re ready.</p></div>'}</section>`);if(latest)requestAnimationFrame(()=>draw(document.querySelector("#arm-chart"),[state.checkins.map(x=>({value:x.rightArm})),state.checkins.map(x=>({value:x.leftArm}))],["#a96875","#718f84"]))}
function plan(){const s=state.settings;app.innerHTML=nav(`<section class="page-head"><p class="eyebrow">Plan</p><h1>Your plan, still yours.</h1><p>Edit targets whenever the plan needs to meet you where you are.</p></section><section class="explainer"><strong>About your checkpoint</strong><p>Your checkpoint is a reassessment point, not necessarily your final goal. When you reach it, review your progress before deciding what comes next.</p></section><form id="plan-form" class="plan-form"><section><h2>Weight & dates</h2><div class="two"><label>Starting weight <small>${s.unit}</small><input required inputmode="decimal" type="number" min="1" step=".1" name="startingWeight" value="${esc(s.startingWeight)}"></label><label>First checkpoint <small>${s.unit}</small><input required inputmode="decimal" type="number" min="1" step=".1" name="checkpointWeight" value="${esc(s.checkpointWeight)}"></label></div><label>Tracking start date<input required type="date" name="startDate" max="${U.localDateString()}" value="${s.startDate}"></label></section><section><h2>Nutrition & movement</h2><div class="two"><label>Daily calories<input required type="number" min="1" name="dailyCalories" value="${s.dailyCalories}"></label><label>Weekly calories<input required type="number" min="1" name="weeklyCalories" value="${s.weeklyCalories}"></label><label>Protein <small>g</small><input required type="number" min="1" name="proteinTarget" value="${s.proteinTarget}"></label><label>Steps<input required type="number" min="1" name="stepTarget" value="${s.stepTarget}"></label></div></section><section><h2>Weekly intentions</h2><div class="two"><label>Lift days, min<input required type="number" min="0" max="7" name="liftMin" value="${s.liftMin}"></label><label>Lift days, max<input required type="number" min="0" max="7" name="liftMax" value="${s.liftMax}"></label><label>Loss range, low<input required type="number" min="0" step=".1" name="lossMin" value="${s.lossMin}"></label><label>Loss range, high<input required type="number" min="0" step=".1" name="lossMax" value="${s.lossMax}"></label></div><label>Week starts<select name="weekStart"><option value="0" ${+s.weekStart===0?"selected":""}>Sunday</option><option value="1" ${+s.weekStart===1?"selected":""}>Monday</option><option value="6" ${+s.weekStart===6?"selected":""}>Saturday</option></select></label></section><button class="primary">Save Plan</button></form><section class="data-card"><div><p class="eyebrow">Privacy & data</p><h2>Kept on this device</h2><p>No account, server storage, analytics, or telemetry. Backups are the safest way to protect your history.</p></div><div><button data-action="export">Export Backup</button><label>Import Backup<input id="import-file" type="file" accept="application/json,.json"></label><button class="danger" data-action="reset">Reset All Data</button></div></section>`)}
function render(){if(!state.settings)return onboarding();({today,progress,checkin,plan}[state.view]||today)()}
const vals=f=>Object.fromEntries(new FormData(f));function settings(d){return{...d,key:"plan",dailyCalories:+d.dailyCalories,weeklyCalories:+d.weeklyCalories,proteinTarget:+d.proteinTarget,stepTarget:+d.stepTarget,liftMin:+d.liftMin,liftMax:+d.liftMax,lossMin:+d.lossMin,lossMax:+d.lossMax,weekStart:+(d.weekStart||0),unit:"lb"}}
document.addEventListener("submit",async e=>{e.preventDefault();const f=e.target,d=vals(f);if(f.id==="setup-form"){state.settings=settings({...defaults,...d});await TrackerDB.put("settings",state.settings);notify("Your tracker is ready.");render()}if(f.id==="daily-form"){await TrackerDB.put("logs",{date:state.date,weight:d.weight===""?null:+d.weight,calories:d.calories===""?null:+d.calories,protein:d.protein===""?null:+d.protein,steps:d.steps===""?null:+d.steps,lifted:d.lifted==="true",note:d.note.trim(),updatedAt:new Date().toISOString()});await load();notify("Today saved.");render()}if(f.id==="checkin-form"){const r={date:d.date,weight:+d.weight,rightArm:+d.rightArm,leftArm:+d.leftArm,waist:+d.waist,note:d.note.trim(),updatedAt:new Date().toISOString()};if(state.checkins.some(x=>x.date===r.date)&&!confirm("Replace the existing check-in for this date?"))return;await TrackerDB.put("checkins",r);await load();notify("Check-in saved.");render()}if(f.id==="plan-form"){state.settings=settings({...state.settings,...d});await TrackerDB.put("settings",state.settings);notify("Plan updated.");render()}});
document.addEventListener("click",async e=>{const v=e.target.closest("[data-view]");if(v){state.view=v.dataset.view;render();scrollTo({top:0,behavior:"smooth"});return}const b=e.target.closest("[data-action]");if(!b)return;const a=b.dataset.action;if(a==="prev-day"){state.date=U.addDays(state.date,-1);render()}if(a==="next-day"&&state.date<U.localDateString()){state.date=U.addDays(state.date,1);render()}if(a==="today-date"){state.date=U.localDateString();render()}if(a==="delete-checkin"&&confirm(`Delete the check-in from ${dateFmt(b.dataset.date)}?`)){await TrackerDB.remove("checkins",b.dataset.date);await load();render();notify("Check-in deleted.")}if(a==="export"){const d=await TrackerDB.snapshot(),blob=new Blob([JSON.stringify(d,null,2)],{type:"application/json"}),link=document.createElement("a");link.href=URL.createObjectURL(blob);link.download=`tracker-backup-${U.localDateString()}.json`;link.click();URL.revokeObjectURL(link.href);notify("Backup exported.")}if(a==="reset"){const x=prompt('This permanently removes all tracker data from this device. Type "RESET" to continue.');if(x==="RESET"){await TrackerDB.clearAll();await load();state.view="today";render();notify("All local data was reset.")}}if(a==="update")navigator.serviceWorker?.getRegistration().then(r=>r?.waiting?.postMessage({type:"SKIP_WAITING"}))});
document.addEventListener("change",async e=>{if(e.target.id!=="import-file"||!e.target.files[0])return;try{const d=JSON.parse(await e.target.files[0].text());TrackerDB.validateBackup(d);if(!confirm(`Import this backup? It contains ${d.logs.length} daily logs and ${d.checkins.length} check-ins. Current local data will be replaced.`))return;await TrackerDB.restore(d);await load();state.view="today";render();notify("Backup restored.")}catch(x){alert(`The backup could not be imported. ${x.message}`)}finally{e.target.value=""}});
document.addEventListener("input",e=>{const form=e.target.closest("#setup-form");if(!form)return;const weekly=form.elements.weeklyCalories;if(e.target===weekly)weekly.dataset.edited="true";if(e.target.name==="dailyCalories"&&weekly.dataset.edited!=="true")weekly.value=Number(e.target.value||0)*7});
async function sw(){if(!("serviceWorker"in navigator))return;const r=await navigator.serviceWorker.register("./service-worker.js");if(r.waiting){state.updateReady=true;render()}r.addEventListener("updatefound",()=>r.installing?.addEventListener("statechange",()=>{if(r.installing?.state==="installed"&&navigator.serviceWorker.controller){state.updateReady=true;render()}}));let refreshing=false;navigator.serviceWorker.addEventListener("controllerchange",()=>{if(!refreshing){refreshing=true;location.reload()}})}
(async()=>{try{await load();render();await sw()}catch(x){app.innerHTML=`<main class="fatal"><h1>Couldn’t open your tracker.</h1><p>${esc(x.message)}</p><button onclick="location.reload()">Try again</button></main>`}})();
