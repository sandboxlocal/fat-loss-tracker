import test from "node:test";import assert from "node:assert/strict";import "../lib.js";const U=globalThis.TrackerUtils;
test("local dates do not shift through UTC",()=>{assert.equal(U.addDays("2026-08-16",1),"2026-08-17");assert.equal(U.addDays("2026-03-08",1),"2026-03-09")});
test("week bounds respect Sunday and Monday starts",()=>{assert.deepEqual(U.weekBounds("2026-08-19",0),{start:"2026-08-16",end:"2026-08-22"});assert.deepEqual(U.weekBounds("2026-08-19",1),{start:"2026-08-17",end:"2026-08-23"})});
test("weekly budget uses food consumed without exercise adjustment",()=>{const settings={weeklyCalories:10500,weekStart:0},logs=[{date:"2026-08-16",calories:1500},{date:"2026-08-17",calories:1800,lifted:true}];const b=U.weeklyBudget(logs,settings,"2026-08-18");assert.equal(b.consumed,3300);assert.equal(b.remaining,7200);assert.equal(b.remainingDays,4);assert.equal(b.perDay,1800)});
test("checkpoint progress works for a downward target",()=>{const p=U.checkpointProgress({startingWeight:200,checkpointWeight:180},190);assert.equal(p.percent,50);assert.equal(p.lost,10);assert.equal(p.remaining,10);assert.equal(p.reached,false)});
test("incomplete week is not assigned a final pace",()=>{const weeks=U.groupWeeks([{date:"2026-08-16",weight:190,calories:1500}],{weekStart:0,weeklyCalories:10500,lossMin:.5,lossMax:1.5},"2026-08-18");assert.equal(weeks[0].complete,false);assert.equal(weeks[0].change,null);assert.equal(weeks[0].pace,"In progress")});
