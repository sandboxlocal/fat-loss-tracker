"use strict";

function onboarding(){
  app.innerHTML=`<main class="onboarding"><div class="onboarding-art"><i></i><i></i><i></i></div><p class="eyebrow">Your progress plan</p><h1>A simple way to follow your plan and see what's working.</h1><p class="lede">Set your starting point and goals once. Your daily logs will do the rest. Everything stays on this device.</p><form id="setup-form" class="setup-form"><section class="setup-card"><header><b>01</b><div><h2>Starting point</h2><p>Where you're starting and the first point where you'll reassess your progress.</p></div></header><div class="two"><label>Starting weight <small>lb</small><input required inputmode="decimal" type="number" min="1" step=".1" name="startingWeight" placeholder="Enter weight"></label><label>First checkpoint <small>lb</small><input required inputmode="decimal" type="number" min="1" step=".1" name="checkpointWeight" placeholder="Reassessment weight"></label></div><label>Tracking start date<input required type="date" name="startDate" value="${defaults.startDate}" max="${U.localDateString()}"></label></section><section class="setup-card"><header><b>02</b><div><h2>Daily rhythm</h2><p>Set the daily targets you'll use to guide your plan.</p></div></header><div class="two"><label>Daily calories<input required inputmode="numeric" type="number" min="1" name="dailyCalories" value="${defaults.dailyCalories}"></label><label>Weekly calories<input required inputmode="numeric" type="number" min="1" name="weeklyCalories" value="${defaults.weeklyCalories}"></label><label>Protein <small>g</small><input required inputmode="numeric" type="number" min="1" name="proteinTarget" value="${defaults.proteinTarget}"></label><label>Steps<input required inputmode="numeric" type="number" min="1" name="stepTarget" value="${defaults.stepTarget}"></label></div></section><section class="setup-card"><header><b>03</b><div><h2>Weekly intentions</h2><p>Set your weekly activity goal and the pace you're aiming for.</p></div></header><div class="two"><label>Lift days, minimum<input required inputmode="numeric" type="number" min="0" max="7" name="liftMin" value="3"></label><label>Lift days, maximum<input required inputmode="numeric" type="number" min="0" max="7" name="liftMax" value="4"></label><label>Loss range, lower <small>lb</small><input required inputmode="decimal" type="number" min="0" step=".1" name="lossMin" value=".5"></label><label>Loss range, upper <small>lb</small><input required inputmode="decimal" type="number" min="0" step=".1" name="lossMax" value="1.5"></label></div></section><section class="setup-card"><header><b>04</b><div><h2>Baseline measurements</h2><p>Optional, but useful if you want to compare body measurements later.</p></div></header><div class="two"><label>Right upper arm <small>in · optional</small><input inputmode="decimal" type="number" min="0" step=".01" name="baselineRightArm" placeholder="—"></label><label>Left upper arm <small>in · optional</small><input inputmode="decimal" type="number" min="0" step=".01" name="baselineLeftArm" placeholder="—"></label></div><label>Waist circumference <small>in · optional</small><input inputmode="decimal" type="number" min="0" step=".01" name="baselineWaist" placeholder="—"></label></section><button class="primary" type="submit">Begin tracking</button><p class="privacy">Stored only in this browser. Export backups regularly.</p></form></main>`;
}

function checkinTiming(){return U.checkinSchedule(state.settings.startDate,state.checkins,U.localDateString())}

function today(){
  const l=logFor(),b=U.weeklyBudget(state.logs,state.settings,state.date),timing=checkinTiming(),count=state.logs.filter(x=>x.date<=U.localDateString()).length,canNext=state.date<U.localDateString();
  app.innerHTML=nav(`<section class="today-head"><div><p class="eyebrow">${state.date===U.localDateString()?"Today":"Daily log"}</p><h1>${dateFmt(state.date)}</h1><p>${count} day${count===1?"":"s"} logged · consistency over perfection</p></div><div class="date-nav"><button data-action="prev-day" aria-label="Previous day">‹</button><button data-action="today-date">Today</button><button data-action="next-day" ${canNext?"":"disabled"} aria-label="Next day">›</button></div></section>${timing.due?'<button class="due-banner" data-view="checkin"><span><strong>Body check-in is due</strong><small>A gentle two-week reminder</small></span><b>›</b></button>':""}<section class="metrics">${metric("Calories",l.calories,state.settings.dailyCalories,"","rose")}${metric("Protein",l.protein,state.settings.proteinTarget,"g","mauve")}${metric("Steps",l.steps,state.settings.stepTarget,"","sage")}</section><section class="weekly"><header><div><p class="eyebrow">Weekly budget</p><h2>${fmt(b.remaining)} <small>calories ${b.remaining>=0?"left":"over"}</small></h2></div><span>${dateFmt(b.start,{month:"short",day:"numeric"})}–${dateFmt(b.end,{month:"short",day:"numeric"})}</span></header><div class="budget-bar"><i style="width:${b.budget?Math.min(100,b.consumed/b.budget*100):0}%"></i></div><footer><span><strong>${fmt(b.consumed)}</strong> of ${fmt(b.budget)} eaten</span><span><strong>${fmt(b.perDay)}</strong>/day for ${b.remainingDays} remaining day${b.remainingDays===1?"":"s"}</span></footer></section><form id="daily-form" class="form-card"><header class="card-head"><div><p class="eyebrow">Daily details</p><h2>Quick log</h2></div>${state.logs.some(x=>x.date===state.date)?'<span class="saved">Saved</span>':""}</header><div class="two"><label>Morning weight <small>${state.settings.unit}</small><input inputmode="decimal" type="number" min="1" step=".1" name="weight" value="${esc(l.weight??"")}" placeholder="—"></label><label>Calories eaten<input inputmode="numeric" type="number" min="0" name="calories" value="${esc(l.calories??"")}" placeholder="—"></label><label>Protein <small>g</small><input inputmode="numeric" type="number" min="0" name="protein" value="${esc(l.protein??"")}" placeholder="—"></label><label>Steps<input inputmode="numeric" type="number" min="0" name="steps" value="${esc(l.steps??"")}" placeholder="—"></label></div><fieldset><legend>Lifted today?</legend><label><input type="radio" name="lifted" value="false" ${!l.lifted?"checked":""}><span>No</span></label><label><input type="radio" name="lifted" value="true" ${l.lifted?"checked":""}><span>Yes</span></label></fieldset><label>Optional note<textarea name="note" rows="3" maxlength="500" placeholder="Anything worth remembering?">${esc(l.note)}</textarea></label><button class="primary" type="submit">Save Today</button></form>`);
}

function baselinePoint(){const s=state.settings,has=[s.baselineRightArm,s.baselineLeftArm,s.baselineWaist].some(v=>U.num(v)!==null);return has?{date:s.startDate,rightArm:U.num(s.baselineRightArm),leftArm:U.num(s.baselineLeftArm),waist:U.num(s.baselineWaist),baseline:true}:null}
function measurementStart(field){const baseline=baselinePoint();return baseline?.[field]??state.checkins.find(x=>U.num(x[field])!==null)?.[field]??null}

function checkin(){
  const latest=state.checkins.at(-1),timing=checkinTiming(),baseline=baselinePoint(),chartPoints=[...(baseline?[baseline]:[]),...state.checkins],timingCard=timing.due?'<section class="due-card"><i></i><div><strong>Body check-in is due</strong><p>Your next two-week check-in is ready.</p></div></section>':`<section class="next"><div><span>Next check-in</span><strong>${dateFmt(timing.nextDate,{month:"long",day:"numeric"})}</strong></div><span>${timing.daysToGo} day${timing.daysToGo===1?"":"s"} to go</span></section>`;
  app.innerHTML=nav(`<section class="page-head"><p class="eyebrow">Body check-in</p><h1>Notice the longer story.</h1><p>A check-in every two weeks is enough. Tiny shifts don’t need a big interpretation.</p></section>${timingCard}<form id="checkin-form" class="form-card"><header class="card-head"><div><p class="eyebrow">New check-in</p><h2>Measurements</h2></div></header><label>Date<input required type="date" name="date" max="${U.localDateString()}" value="${U.localDateString()}"></label><div class="two"><label>Weight <small>${state.settings.unit}</small><input required inputmode="decimal" type="number" min="1" step=".1" name="weight"></label><label>Right upper arm <small>in</small><input required inputmode="decimal" type="number" min="0" step=".01" name="rightArm"></label><label>Left upper arm <small>in</small><input required inputmode="decimal" type="number" min="0" step=".01" name="leftArm"></label><label>Waist <small>in</small><input required inputmode="decimal" type="number" min="0" step=".01" name="waist"></label></div><label>Optional note<textarea name="note" rows="3" maxlength="500"></textarea></label><button class="primary">Save Check-In</button></form>${latest?`<section class="arm-card"><div class="section-title"><p class="eyebrow">Arm measurements</p><h2>Baseline to latest</h2></div><div class="arm-grid"><article><span>Right arm</span><strong>${fmt(latest.rightArm,2)} in</strong><p>${signed(latest.rightArm-measurementStart("rightArm")," in")} since ${baseline?.rightArm!=null?"baseline":"first check-in"}</p></article><article><span>Left arm</span><strong>${fmt(latest.leftArm,2)} in</strong><p>${signed(latest.leftArm-measurementStart("leftArm")," in")} since ${baseline?.leftArm!=null?"baseline":"first check-in"}</p></article></div><canvas id="arm-chart" height="190"></canvas></section>`:""}<section class="history"><div class="section-title"><p class="eyebrow">History</p><h2>Previous check-ins</h2></div>${state.checkins.length?state.checkins.slice().reverse().map(x=>`<article class="check-row"><div><strong>${dateFmt(x.date,{month:"short",day:"numeric",year:"numeric"})}</strong><span>${fmt(x.weight,1)} ${state.settings.unit} · Waist ${fmt(x.waist,2)} in</span></div><div><span>R ${fmt(x.rightArm,2)}</span><span>L ${fmt(x.leftArm,2)}</span><button data-action="delete-checkin" data-date="${x.date}" aria-label="Delete check-in">×</button></div></article>`).join(""):'<div class="empty"><strong>Your first comparison starts here.</strong><p>Add a check-in when the first two-week interval arrives.</p></div>'}</section>`);
  if(latest)requestAnimationFrame(()=>draw(document.querySelector("#arm-chart"),[chartPoints.filter(x=>x.rightArm!=null).map(x=>({value:x.rightArm})),chartPoints.filter(x=>x.leftArm!=null).map(x=>({value:x.leftArm}))],["#a96875","#718f84"]));
}

function settings(d){const optional=(name)=>Object.prototype.hasOwnProperty.call(d,name)?(d[name]===""?null:U.num(d[name])):state.settings?.[name]??null;return{...d,key:"plan",dailyCalories:+d.dailyCalories,weeklyCalories:+d.weeklyCalories,proteinTarget:+d.proteinTarget,stepTarget:+d.stepTarget,liftMin:+d.liftMin,liftMax:+d.liftMax,lossMin:+d.lossMin,lossMax:+d.lossMax,baselineRightArm:optional("baselineRightArm"),baselineLeftArm:optional("baselineLeftArm"),baselineWaist:optional("baselineWaist"),weekStart:+(d.weekStart||0),unit:"lb"}}
